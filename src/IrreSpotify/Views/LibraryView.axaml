<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:IrreSpotify.ViewModels"
             xmlns:models="using:IrreSpotify.Models"
             xmlns:asyncImageLoader="clr-namespace:AsyncImageLoader;assembly=AsyncImageLoader.Avalonia"
             x:Class="IrreSpotify.Views.LibraryView"
             x:DataType="vm:LibraryViewModel">

    <Grid RowDefinitions="Auto, *">
        <!-- Header -->
        <Border Grid.Row="0" Padding="24,20" Background="#09090B">
            <Grid ColumnDefinitions="*, Auto">
                <StackPanel Grid.Column="0" Spacing="6">
                    <TextBlock Text="Your Library" FontSize="26" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#A7A7A7"/>
                </StackPanel>
                <Button Grid.Column="1" 
                        Command="{Binding LoadLibraryCommand}" 
                        Content="🔄 Refresh" 
                        Background="#18181C" 
                        Foreground="White" 
                        CornerRadius="16" 
                        Padding="14,8" 
                        FontSize="12"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Loading Progress Bar -->
        <ProgressBar Grid.Row="1" 
                     IsIndeterminate="True" 
                     IsVisible="{Binding IsLoading}" 
                     Height="3" 
                     VerticalAlignment="Top" 
                     Foreground="#1DB954" 
                     Background="Transparent"/>

        <!-- Playlists Items Control / List -->
        <ScrollViewer Grid.Row="1" Padding="24,20">
            <ItemsControl ItemsSource="{Binding Playlists}" Background="Transparent">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:PlaylistItem">
                        <Border Margin="0,0,16,16" Width="180" Height="240" CornerRadius="12" Background="#18181C" Padding="14">
                            <Grid RowDefinitions="150, *, Auto">
                                <!-- Cover Container -->
                                <Border Grid.Row="0" CornerRadius="8" ClipToBounds="True" Background="#24242A" HorizontalAlignment="Stretch">
                                    <Grid>
                                        <TextBlock Text="📁" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <Image asyncImageLoader:ImageLoader.Source="{Binding CoverUrl}" Stretch="UniformToFill"/>
                                    </Grid>
                                </Border>

                                <!-- Title -->
                                <TextBlock Grid.Row="1" 
                                           Text="{Binding Name}" 
                                           FontWeight="Bold" 
                                           Foreground="White" 
                                           FontSize="14" 
                                           Margin="0,8,0,0" 
                                           TextTrimming="CharacterEllipsis"/>

                                <!-- Owner & Play Button -->
                                <Grid Grid.Row="2" ColumnDefinitions="*, Auto" Margin="0,4,0,0">
                                    <TextBlock Grid.Column="0" 
                                               Text="{Binding Owner}" 
                                               FontSize="12" 
                                               Foreground="#A7A7A7" 
                                               VerticalAlignment="Center" 
                                               TextTrimming="CharacterEllipsis"/>

                                    <Button Grid.Column="1" 
                                            Command="{Binding $parent[UserControl].((vm:LibraryViewModel)DataContext).PlayPlaylistCommand}" 
                                            CommandParameter="{Binding}" 
                                            Background="#1DB954" 
                                            Foreground="Black" 
                                            CornerRadius="16" 
                                            Width="32" 
                                            Height="32" 
                                            Padding="0" 
                                            Cursor="Hand">
                                        <TextBlock Text="▶" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12"/>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
